# Contributing Guide

## Pinia

- We use the alternative, composable syntax for Pinia: https://pinia.vuejs.org/core-concepts/#Setup-Stores

- API responses **that are transformed into state objects/fields** needn't be typed since TypeScript's utility lies in the local development, and can't enforce the API responses' types
